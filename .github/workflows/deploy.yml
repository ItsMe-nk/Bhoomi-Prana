name: Deploy to GitHub Pages
on:
  push:
    branches: [main]

# MOVE THIS TO THE TOP LEVEL
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # ... rest of your steps ...
