name: Deploy to GitHub Pages
on:
  push:
    branches: [main]

# Move these to the top level (outside of 'jobs')
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # ... rest of your code
